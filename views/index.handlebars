<div class="row">
  <div class="col s12 scraperTag">
    <div class="z-depth-3 center-align heading">Extra! <a href="scrape"><img src="assets/images/Scraper.png" id="scraper"></a> Extra!</div>
    <h5 class="center-align byline">Click the Scraper to Scrape some Reading Related Reports!</h5>
    <div class="row">
      <div class="col s4">
        Something here  
      </div>
      <div class="col s4 center-align">
        Something else here
      </div>
      <div class="col s4 right-align">
        Something Here too
      </div>
    </div>
  </div>
</div>


<div class="row">
  
  <div class ="col s12 left-align box">
    {{#each article}}
      <div class="card-panel">
        <h4>{{this.title}}</h4>
        <p>By: {{this.author}}</p>
        {{!-- goes to article.handlebars --}}
        <a href = "/readArticle/{{this._id}}" class="waves-effect waves-light btn red darken-4" id="btnSignUp">Read Quick Article</a>
      </div>
    {{/each}}
  </div>
</div>